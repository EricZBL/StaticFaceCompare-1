server:
  port: 8888
  servlet:
    context-path: /api
spring:
  data:
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: ${elasticsearch.host}
      repositories:
        enabled: true
zk:
  address: ${zk.address}

hzgc:
  person:
    dir: /persondir
    sim: 80
server:
  port: 8083
spring:
  data:
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: ${elasticsearch.host}
      repositories:
        enabled: true
zk:
  address: ${zk.address}
logging:
  config: classpath:logback-boot.xml

hzgc:
  person:
    dir: /persondir
    sim: 80